@model List<BibleVerse>


<div class="row">
    @if(Model != null && Model.Any())
    {
        for (int i = 0; i < Model.Count; i++)
        {
            <div class="col-12">
                <p>@Model[i].Verse - @Model[i].Text</p>
            </div>
        }
    }
</div>